---
description: "Coordinate a development task with full workflow"
argument-hint: "development task description"
---

Coordinate a complete development workflow for: $ARGUMENTS

Follow the 18-step AI Development Framework with proper planning, quality gates, and TaskCreate/TaskUpdate progress tracking. Use built-in agents (Explore, Plan, general-purpose) for the workflow and custom agents (test-specialist, quality-guardian, review-coordinator) at their designated phases.

For spec-driven features, use the spec-kit pipeline: `/speckit.specify` -> `/speckit.plan` -> `/speckit.tasks` -> `/speckit.implement`.
